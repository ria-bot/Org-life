export default function AdminDashboard() {
  return (
    <div>
      <h1>Admin Dashboard</h1>
      <p>Placeholder — we'll build this out with user management, transactions, reports, etc.</p>
    </div>
  );
}